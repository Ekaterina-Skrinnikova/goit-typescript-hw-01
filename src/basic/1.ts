// const age = 50;
// const username = "Max";
// const toggle = true;
// const empty = null;
// const callback = (a) => {
//   return 100 + a;
// };

//  передаються данні явно тому можно не вказувати тип
const age = 50;
const username = "Max";
const toggle = true;
const empty = null;

let callback = (a: number) => {
  return 100 + a;
};
